import pygame

class Boss1():
    def __init__(self,position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/MARIO_VINTAGE_2.png")
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO1():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO2():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO4():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO5():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO6():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO7():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO8():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO9():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO10():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO11():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO12():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO13():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego5.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO14():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego4.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO15():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego4.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO16():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego4.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO17():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego4.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO18():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego4.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO19():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bola_fuego4.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM2():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM4():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM5():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM6():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM7():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM8():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM9():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM10():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM11():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM12():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM13():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM14():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM15():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM16():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM17():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM18():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BUM19():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class RELOJ():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/RELOJ.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class RELOJ2():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/RELOJ.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class RELOJ3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/RELOJ.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class RELOJ4():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/RELOJ.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position
