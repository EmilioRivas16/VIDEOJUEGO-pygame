import pygame

class Boss3():
    def __init__(self,position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/BOWSER(VINTAGE)3.png")
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO1_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO2_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO3_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO4_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO5_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO6_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO7_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO8_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO9_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO10_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO11_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO12_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO13_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER2.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO14_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER3.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO15_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER3.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO16_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER3.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO17_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER3.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO18_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER3.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position

class BOLA_FUEGO19_3():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/FUEGO_BOWSER3.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position



class BUM():
    def __init__(self, position):
        self.sheet = pygame.image.load("IMAGENES_VIDEOJUEGO/bum.png").convert_alpha()
        self.image = self.sheet.subsurface(self.sheet.get_clip())
        self.rect = self.image.get_rect()
        self.rect.topleft = position
